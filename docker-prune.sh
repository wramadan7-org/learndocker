## Docker prune is to delete the network or container or volume or image when it not used
docker nameofcommand(network, volume, image, container) prune

## If we want to delete all but not the volume use this command
docker system prune